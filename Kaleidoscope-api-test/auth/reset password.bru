meta {
  name: reset password
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "token":"447784",
    "newPassword":"prabhuajay"
  }
}
